import 'package:flutter/material.dart';

class WeatherSummary extends StatelessWidget {
  const WeatherSummary({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('i am req'),
    );
  }
}
